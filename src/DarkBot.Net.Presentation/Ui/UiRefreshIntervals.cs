namespace DarkBot.Net.Presentation.Ui;

/// <summary>Единые интервалы опроса UI (title bar, main dashboard).</summary>
internal static class UiRefreshIntervals
{
    public static readonly TimeSpan Dashboard = TimeSpan.FromMilliseconds(250);
}
